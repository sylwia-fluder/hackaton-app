import React from 'react';
import classNames from 'classnames';
import styles from './sprintDetails.scss';

const SprintDetails = () => {
    return(
        <div>
            Sprint details
        </div>
    );
};

export default SprintDetails;

